class Comment {
    constructor(
        private id: string,
        private user_id: string,
        private post_id: string,
        private comment: string
    ){
    }
 }

 export default Comment 